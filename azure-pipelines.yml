# ============================================
#   Build, Unit Test & Deploy
#   EasyHMS API (.NET 8) -> Azure App Service (WINDOWS)
#
#   Dev deploy : release-* AND feature/*
#   Prod deploy: release-* only + deployProd=true + env approval
#   PR builds  : Build + Tests only (no deploy)
#
#   NOTE: Your Kudu trace shows Windows paths (C:\home\...),
#         so appType is set to webApp (Windows).
# ============================================

parameters:
- name: deployProd
  type: boolean
  default: false

trigger:
  branches:
    include:
      - 'release-*'
      - 'feature/*'

pr:
  branches:
    include:
      - 'release-*'
      - 'feature/*'

pool:
  vmImage: 'ubuntu-latest'

variables:
  # Projects
  apiProject: 'EasyHMSAPI.Api/EasyHMSAPI.Api.csproj'
  testProjects: '**/*UnitTests/*.csproj'

  # Artifacts
  publishDir: '$(Build.ArtifactStagingDirectory)/publish'
  artifactName: 'api-drop'

  # Unit test results
  testResultsDir: '$(Build.SourcesDirectory)/TestResults'

  # Dev
  devAppName: 'easyHMSAPIServices'
  devServiceConnection: 'svc-conn-nexeagle-dev'

  # Prod (fix spelling to your real app service name!)
  prodAppName: 'easyHMSAPIServicesProd'
  prodServiceConnection: 'svc-conn-nexeagle-prod'

stages:

# ================================================================
#  STAGE 1 — Build + Unit Tests
# ================================================================
- stage: Build_Test
  displayName: Build + Unit Tests
  jobs:
  - job: BuildTestJob
    displayName: Build/Test
    steps:
    - checkout: self

    - task: UseDotNet@2
      displayName: 'Use .NET 8 SDK'
      inputs:
        version: '8.x'

    - task: DotNetCoreCLI@2
      displayName: 'Restore API'
      inputs:
        command: 'restore'
        projects: '$(apiProject)'

    - task: DotNetCoreCLI@2
      displayName: 'Restore Tests'
      inputs:
        command: 'restore'
        projects: '$(testProjects)'

    - task: DotNetCoreCLI@2
      displayName: 'Build API'
      inputs:
        command: 'build'
        projects: '$(apiProject)'
        arguments: '--configuration Release --no-restore'

    - task: DotNetCoreCLI@2
      displayName: 'Unit Tests'
      inputs:
        command: 'test'
        projects: '$(testProjects)'
        arguments: >
          --configuration Release
          --logger "trx"
          --results-directory "$(testResultsDir)"
          -- RunConfiguration.TreatNoTestsAsError=true
        publishTestResults: false

    - task: PublishTestResults@2
      displayName: 'Publish Unit Test Results'
      inputs:
        testResultsFormat: 'VSTest'
        testResultsFiles: '$(testResultsDir)/**/*.trx'
        testRunTitle: 'Unit Tests'
      condition: succeededOrFailed()

    # Publish output (zip) to artifact
    - task: DotNetCoreCLI@2
      displayName: 'Publish API (zip)'
      inputs:
        command: 'publish'
        projects: '$(apiProject)'
        arguments: '--configuration Release --no-restore --output $(publishDir)'
        publishWebProjects: false
        zipAfterPublish: true

    - publish: '$(publishDir)'
      artifact: '$(artifactName)'

# ================================================================
#  STAGE 2 — Deploy DEV (release-* and feature/*, NOT on PR)
# ================================================================
- stage: Deploy_Dev
  displayName: Deploy to App Service (Dev)
  dependsOn: Build_Test
  condition: >
    and(
      succeeded(),
      ne(variables['Build.Reason'], 'PullRequest'),
      or(
        startsWith(variables['Build.SourceBranch'], 'refs/heads/release-'),
        startsWith(variables['Build.SourceBranch'], 'refs/heads/feature/')
      )
    )
  jobs:
  - deployment: DeployDev
    displayName: Deploy Dev
    environment: 'dev'
    strategy:
      runOnce:
        deploy:
          steps:
          - download: current
            artifact: $(artifactName)

          # Use Run-From-Package (more stable; avoids heavy extraction failures)
          - task: AzureWebApp@1
            displayName: 'Deploy ZIP to Dev App Service'
            inputs:
              azureSubscription: '$(devServiceConnection)'
              appType: 'webApp'     # Windows App Service
              appName: '$(devAppName)'
              package: '$(Pipeline.Workspace)/$(artifactName)/**/*.zip'
              deploymentMethod: 'runFromPackage'

# ================================================================
#  STAGE 3 — Deploy PROD (release-* + deployProd=true + approval)
#  Configure approvals in Pipelines → Environments → prod
# ================================================================
- stage: Deploy_Prod
  displayName: Deploy to App Service (PROD) — Environment Approval
  dependsOn: Deploy_Dev
  condition: >
    and(
      succeeded(),
      ne(variables['Build.Reason'], 'PullRequest'),
      startsWith(variables['Build.SourceBranch'], 'refs/heads/release-'),
      eq(${{ parameters.deployProd }}, true)
    )
  jobs:
  - deployment: DeployProd
    displayName: Deploy PROD
    environment: 'prod'
    strategy:
      runOnce:
        deploy:
          steps:
          - download: current
            artifact: $(artifactName)

          - task: AzureWebApp@1
            displayName: 'Deploy ZIP to PROD App Service'
            inputs:
              azureSubscription: '$(prodServiceConnection)'
              appType: 'webApp'     # Windows App Service
              appName: '$(prodAppName)'
              package: '$(Pipeline.Workspace)/$(artifactName)/**/*.zip'
              deploymentMethod: 'runFromPackage'
